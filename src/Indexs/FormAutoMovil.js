import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import AppAutoMovilForm from '../App/AppAutoMovilForm';

ReactDOM.render(
  <React.StrictMode>
    <AppAutoMovilForm />
  </React.StrictMode>,
  document.getElementById('rootAutmovilForm')
);

